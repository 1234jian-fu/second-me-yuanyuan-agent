# Second Me / 数字分身 Agent
# 1:1 UI 还原总规则包

## 1. 项目目标
本仓库当前的核心任务不是“自由发挥设计”，而是：
**尽可能 1:1 还原我提供的移动端 UI 参考图，并保证代码结构可维护、素材可替换、交互可扩展。**

你必须优先遵守：
1. 先还原布局与视觉层级
2. 再还原组件状态与交互
3. 最后再补工程优化
4. 不允许擅自新增页面中没有的内容
5. 不允许随意改变信息层级、视觉重心、配色气质

---

## 2. 你的角色
你是一个“UI 还原工程师”，不是重新设计产品的设计师。

你的职责：
- 忠实还原参考图
- 把 UI 拆成可维护组件
- 预留素材替换能力
- 保持风格统一
- 输出可运行代码

你的职责不包括：
- 自己发明新的视觉语言
- 擅自增加功能块
- 擅自改变配色体系
- 把简洁页面做复杂
- 在没有要求时重构业务逻辑

---

## 3. 总体工作原则
### 3.1 还原优先级
永远按以下优先级执行：
1. 页面结构
2. 间距与留白
3. 组件尺寸
4. 圆角、阴影、描边
5. 颜色与渐变
6. 字号、字重、行高
7. 图标与插画位置
8. 动效与交互

### 3.2 先计划后编码
如果任务涉及整页还原、多状态还原、主题抽象、公共组件抽取，先输出计划，再开始写代码。

计划至少包括：
- 当前页面拆分方案
- 需要新增的组件
- 需要新增或修改的 theme token
- 占位素材方案
- 验收方式

### 3.3 小步提交
一次只处理一个明确页面或一个明确模块。
不要一口气改 5~7 个页面。
优先保证单页质量和一致性。

---

## 4. UI 还原硬规则
### 4.1 禁止项
以下行为默认禁止：
- 擅自新增参考图中没有的模块
- 擅自改变页面主次顺序
- 擅自更换主色
- 擅自修改文案语气
- 擅自增加复杂图表、复杂边框、复杂阴影
- 擅自把单页改成更“炫”的版本
- 使用与项目整体不一致的图标风格
- 把占位素材直接写死在页面组件内部

### 4.2 必守项
以下行为必须遵守：
- 所有页面遵循统一设计 token
- 所有颜色、圆角、阴影、间距从 theme 层读取
- 所有视觉素材必须可替换
- 所有页面必须适配 iPhone 安全区
- 所有关键组件都应抽离复用
- 默认优先使用简单、稳妥、可维护实现

---

## 5. 设计风格基线
### 5.1 视觉气质
产品视觉气质固定为：
- 温柔
- 治愈
- 轻陪伴
- 轻成长感
- 低饱和
- 干净
- 圆润
- 成熟但不商务
- 可爱但不幼稚

### 5.2 配色基线
总体色彩方向固定为：
- 奶油白 / 暖米白背景
- 浅紫作为核心陪伴色
- 浅黄、浅绿、浅橙作为功能辅助色
- 灰黑文字用于主层级
- 不允许引入高饱和霓虹色、纯黑大面积背景、强科技蓝

### 5.3 组件基线
- 大圆角卡片
- 柔和阴影
- 轻描边
- 足够留白
- 图标和插画柔和统一
- 页面重点内容只能有一个主视觉中心

---

## 6. mascot 与拟物图标规则
### 6.1 mascot 规则
mascot 是产品核心视觉角色，必须：
- 统一世界观
- 统一脸部风格
- 统一体积感
- 统一材质感
- 不允许不同页面出现风格完全不同的 mascot

### 6.2 mascot 使用规则
- 仅在关键视觉区出现
- 首页、心情打卡、录音页、AI 对话页、计划页可以使用
- 历史页、设置页、资料页应减少使用频率
- 不允许每个角落都放 mascot

### 6.3 图标规则
- 先用占位图标实现
- 后续可替换为 SVG / PNG
- 所有图标尺寸、容器圆角、阴影风格统一
- 不允许混用多种风格图标库造成视觉不统一

---

## 7. 资源管理规则
所有素材必须从统一目录读取，不允许散落在页面中。

建议目录：
- assets/mascot/
- assets/icons/
- assets/decor/
- assets/illustrations/

建议封装：
- components/Mascot.tsx
- components/AppIconCard.tsx
- components/FeatureCard.tsx
- components/GradientButton.tsx
- components/BottomTabBar.tsx

当前没有正式 PNG / SVG 时：
- 可以使用 RN 组件
- 可以使用 emoji / Material Icons 临时占位
- 但必须保持“后续一键替换”能力

---

## 8. theme 设计 token 规则
所有页面样式必须优先走 theme，而不是写死。

至少维护以下文件：
- theme/colors.ts
- theme/spacing.ts
- theme/radius.ts
- theme/shadows.ts
- theme/typography.ts

必须抽离：
- 主背景色
- 卡片背景色
- 主文本色
- 次文本色
- 主按钮渐变
- 功能卡片配色
- tab 高亮色
- 默认圆角值
- 页面间距

---

## 9. 页面还原顺序
每个页面都按以下流程工作：

### Step 1 读取参考
先分析参考图中的：
- 页面结构
- 主次层级
- 视觉中心
- 卡片尺寸关系
- 色块关系
- 组件位置
- 字号层级

### Step 2 切分组件
先判断哪些内容应抽成组件：
- 顶部栏
- 大卡片
- 功能入口卡
- 情绪按钮
- 聊天气泡
- 计划列表项
- 统计卡片
- 底部导航

### Step 3 先搭骨架
优先完成：
- 页面布局
- 间距
- 卡片
- 文本层级

### Step 4 再补视觉
补：
- 色彩
- 渐变
- 图标
- mascot
- 装饰元素

### Step 5 最后补交互
补：
- 选中态
- hover/press 态
- tab 切换
- 输入框状态
- 录音状态
- 聊天发送状态

---

## 10. 每次任务的输出格式
每次执行 UI 还原任务时，请严格按以下顺序输出：

1. 任务理解
2. 还原计划
3. 将修改哪些文件
4. 新增哪些组件
5. 代码实现
6. 本地运行方式
7. 验收说明
8. 下一步建议

不要直接无说明地吐出大量代码。

---

## 11. 单页验收标准
一个页面只有在满足以下条件时才算完成：

### 11.1 视觉验收
- 与参考图结构高度一致
- 主视觉位置一致
- 卡片尺寸关系合理
- 颜色和气质一致
- 不出现多余模块
- 不显得比参考图更乱

### 11.2 工程验收
- 无报错
- 样式不写死在单一超长文件里
- 关键组件已抽离
- 主题变量已归位
- 素材可替换
- 代码可继续迭代

### 11.3 交互验收
- 主要点击区可点击
- 输入框可输入
- tab 切换正常
- 页面滚动正常
- 不出现明显溢出和错位

---

## 12. 对截图还原的默认假设
如果只有截图，没有标注稿，请遵守以下默认假设：
- 保持截图中的视觉比例关系
- 不随意缩放主视觉
- 优先保留留白和层级
- 对尺寸不确定时，宁可克制，不要做满
- 对颜色不确定时，优先选低饱和柔和版本
- 对图标不确定时，优先用占位，但保留容器结构

---

## 13. 文案规则
- 优先保留我提供的中文文案
- 不擅自把中文替换成英文
- 不随意扩写副文案
- 不写营销腔
- 不写过度“治愈鸡汤”
- 文案气质保持温柔、简洁、陪伴感

---

## 14. 禁止“设计漂移”
如果你发现后续页面和前面页面风格不一致，必须优先向前面对齐，而不是重新发明风格。

风格统一优先级高于单页精致度。

---

## 15. 推荐工作模式
对于复杂页面，优先使用以下模式：
- 先输出 plan
- 再实现布局
- 再实现视觉
- 再补交互
- 再等我确认

不要一次性大改全部页面。

---

## 16. 完成定义
“完成”不代表你觉得代码写完了，而是：
**用户看到页面后，会觉得它与参考图足够接近，并且后续能继续替换素材、继续扩展。**

如果“更像参考图”和“更工程优雅”冲突，优先保证页面更像参考图，但不能牺牲后续可维护性。
